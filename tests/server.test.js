describe('Server Tests', () => {
  test('should pass basic server test', () => {
    expect(true).toBe(true);
  });

  test('should pass another basic server test', () => {
    expect(2 + 2).toBe(4);
  });

  test('should pass basic server test 3', () => {
    expect(3 + 3).toBe(6);
  });
});
